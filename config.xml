<?xml version="1.0" encoding="UTF-8"?>

<!-- config.xml reference: https://build.phonegap.com/docs/config-xml -->
<widget xmlns = "http://www.w3.org/ns/widgets"
        xmlns:gap = "http://phonegap.com/ns/1.0"
        id = "com.certified.certifiedlocal"
        version = "1.0.0">

    <name>Certified Local - Florida Edition</name>

    <description>
        App that is a game to find all the "jewels" in Florida rising in rank from "Recluse" to "Certified Local".
    </description>

    <author href="http://phonegap.com" email="support@phonegap.com">
        Sarah Daugherty
    </author>

   
    <feature name="http://api.phonegap.com/1.0/device" />
	<feature name="http://api.phonegap.com/1.0/geolocation" />


	<gap:platform name="android" />

    <!-- Customize your app and platform with the preference element. -->
    <preference name="phonegap-version" value="3.5.0" />  <!-- all: current version of PhoneGap -->
    <preference name="target-device" value="universal" /> <!-- all: possible values handset, tablet, or universal -->
    <preference name="fullscreen" value="true" /> <!-- all: hides the status bar at the top of the screen -->
    <preference name="android-minSdkVersion" value="7" /> <!-- android: MIN SDK version supported on the target device. MAX version is blank by default. -->
    <preference name="android-installLocation" value="auto" /> <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->

 
    <!-- Define app icon for each platform. -->
    <icon src="icon.png" />
    <icon src="images/icon/icon-36-ldpi.png" gap:platform="android" gap:density="ldpi" />
    <icon src="images/icon/icon-48-mdpi.png" gap:platform="android" gap:density="mdpi" />
    <icon src="images/icon/icon-72-hdpi.png" gap:platform="android" gap:density="hdpi" />
    <icon src="res/icon/icon-96-xhdpi.png" gap:platform="android" gap:density="xhdpi" />
    

    <!-- Define app splash screen for each platform. -->
	<gap:splash src="splash.png" />
    <gap:splash src="images/splash/ldpi-portrait.png" gap:platform="android" gap:density="ldpi" />
    <gap:splash src="images/splash/mdpi-portrait.png" gap:platform="android" gap:density="mdpi" />
    <gap:splash src="images/splash/hdpi-portrait.png" gap:platform="android" gap:density="hdpi" />
    <gap:splash src="images/splash/xhdpi-portrait.png" gap:platform="android" gap:density="xhdpi" />
  
    <!--
Define access to external domains.

<access /> - a blank access tag denies access to all external resources.
<access origin="*" /> - a wildcard access tag allows access to all external resource.

Otherwise, you can specify specific domains:
-->
    <access origin="http://127.0.0.1*"/> <!-- allow local pages -->
    <!--
<access origin="http://phonegap.com" /> - allow any secure requests to http://phonegap.com/
<access origin="http://phonegap.com" subdomains="true" /> - same as above, but including subdomains, such as http://build.phonegap.com/
<access origin="http://phonegap.com" browserOnly="true" /> - only allows http://phonegap.com to be opened by the child browser.
-->

</widget>



